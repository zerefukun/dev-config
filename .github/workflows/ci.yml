name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: [self-hosted, linux]
    timeout-minutes: 10

    # The replay gate's fixtures are two databases and what each ends up
    # holding, so the suite drives a real Postgres — see tests/replay.test.ts
    # for why nothing in-process can stand in. Pinned by digest for the same
    # reason the actions are.
    #
    # `127.0.0.1::` is the `ip::containerPort` form — loopback, and a host port
    # the daemon picks. See check.yml's database job for why both halves matter
    # on a self-hosted runner; the short of it is that a bare `5432:5432` is
    # reachable from off the box, and that two of these jobs cannot both have
    # it. Every URL below is built from the assignment rather than from a
    # number, which is also what makes TEST_DATABASE_URL explicit here.
    services:
      postgres:
        image: postgres:16-alpine@sha256:20edbde7749f822887a1a022ad526fde0a47d6b2be9a8364433605cf65099416
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 127.0.0.1::5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 30

      # The limiter conformance suite grades a bucket two processes share and a
      # limiter whose Redis is gone, neither of which anything in-process can
      # stand in for. It is flushed between cases and refuses to guess at an
      # address, so this instance exists for that suite and nothing else and the
      # step below names it.
      redis:
        image: redis:7-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf
        ports:
          - 127.0.0.1::6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 30

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # gitleaks reads history, and a shallow clone has none.
          fetch-depth: 0
          # Nothing here pushes, and the job runs third-party code (an
          # unpinned npx, a scaffolded tree's dependencies) in this workspace.
          persist-credentials: false

      - uses: ./.github/actions/secret-scan

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      # This repo runs its own gates on itself, from the working tree. A gate
      # its author's repo cannot pass is a gate nobody should be asked to.
      #
      # Five facts here are structural rather than unfinished: the configs
      # inherit from this repo's own bases by relative path because a package
      # cannot extend itself by name, CI is this file rather than a call into
      # check.yml because a commit cannot pin its own SHA, there is no
      # environment to shape — nothing here reads a secret at runtime — and
      # there is no mutation lane, because this repo is gate scripts and has no
      # pure domain layer for one to mutate. Its suite drives that gate against
      # fixture repositories instead, which is the only place it can be run
      # honestly here.
      #
      # The fifth is the test-suite action, which the step below substitutes for
      # rather than uses. Its seal puts the run in a network namespace, and the
      # Postgres and Redis this suite needs are service containers on the host's
      # loopback, which that namespace does not reach. Substituting drops three
      # guarantees every other repo gets — the seal itself, the junit report,
      # and the two greps over it that refuse a skipped test and an
      # assertion-free one — so this repo is the one repo not held to them.
      # dev-config#58 is where that is answered.
      - uses: ./.github/actions/repo-contract
        with:
          exemptions: config-lineage ci-call secrets

      - uses: ./.github/actions/stack-gate

      # Two suites write directives as fixture text: the hygiene suite, whose
      # job is to contain them, and the React suite, which drives the compiler's
      # rule against an `eslint-disable` a fixture has to spell without a reason.
      - uses: ./.github/actions/suppression-hygiene
        with:
          fixtures: tests/suppression-hygiene.test.ts tests/lint-policy-react.test.ts

      # This repo's own shell is the three libraries the composites source, and
      # the gate that reads a consuming repo's scripts reads them here.
      - uses: ./.github/actions/shell-scripts

      - run: bun install --frozen-lockfile

      # The invariant sweep this repo exports is a Playwright fixture, and what
      # its suite grades is what a browser reports — so the suite drives one.
      # The headless shell rather than the full chromium: nothing here opens a
      # window, and it is the smaller download.
      #
      # No `--with-deps`: it shells out to `sudo apt-get`, and the runner user
      # here may not run apt. The shared libraries it would install are already
      # on the box, which is what lets the browser suites pass without it.
      - run: bunx playwright install chromium-headless-shell

      # The gates this repo ships are TypeScript, and their suite is what proves
      # each one refuses a violating tree and passes a clean one. Every other
      # repo's gate is only as honest as these two lanes.
      - run: bun run check
      # `--coverage` by hand: it is the one guarantee of the substituted action
      # this step does keep, and without it the floor above it declares is
      # applied to nothing. docs/gates/test-suite.md.
      - run: bun test --coverage
        env:
          # Neither has a default, deliberately: the conformance suite flushes
          # its Redis and the database suites drop databases on their server,
          # and `localhost` on a box that runs the fleet's stacks is not a safe
          # thing for either to assume. Both are the port the daemon assigned.
          TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:${{ job.services.postgres.ports['5432'] }}/postgres
          TEST_REDIS_URL: redis://127.0.0.1:${{ job.services.redis.ports['6379'] }}

      # The k6 ramp this repo ships is the one file its own linter cannot read,
      # so it gets executed instead: both branches, against a stub server.
      - name: Execute the capacity ramp
        run: |
          set -euo pipefail
          # shellcheck source=/dev/null
          . .github/actions/_lib/k6.sh
          bun tests/capacity-script.ts

      # Every JSON file nothing else here reads. What is skipped is a file some
      # other tool already parses, in a dialect `JSON.parse` refuses: oxlint's
      # schema declares `allowComments` and `allowTrailingCommas` and TypeScript
      # has always taken both, and `bun run check` above fails loudly on a broken
      # one. So the skip has to match where that tool actually looks. oxlint
      # reads a config from the directory it is linting, at any depth, and the
      # base it extends with it; `tsc` here is given the root project alone, so a
      # tsconfig in a subdirectory is parsed by nothing and belongs in this step.
      - name: Parse every JSON config
        run: |
          set -euo pipefail
          # NUL-delimited, the way `repoFiles` reads a listing: a path outside
          # ASCII comes back quoted with escapes in it under the default
          # `core.quotepath`, and a newline in one would be two entries. Neither
          # is a name this repo has, and both are names a consumer could.
          mapfile -t -d '' files < <(git -c core.quotepath=false ls-files -z '*.json' |
            grep -zvE '((^|/)(\.oxlintrc|oxlint\.base)\.json|^tsconfig(\.[^/]+)?\.json)$')
          # The ${} below are JS template literals, not shell expansions.
          # shellcheck disable=SC2016
          node -e '
            const { readFileSync } = require("node:fs");
            for (const file of process.argv.slice(1)) {
              try {
                JSON.parse(readFileSync(file, "utf8"));
              } catch (error) {
                console.log(`::error file=${file}::${error.message}`);
                process.exitCode = 1;
              }
            }
          ' "${files[@]}"

      # check.yml and the composite actions are the executable half of this
      # repo: a schema error in one of them breaks every consumer at once. The
      # action used here is this repo's own, at this commit — the repos that
      # scaffold from the template reach the same one by SHA.
      - uses: ./.github/actions/lint-workflows

      # actionlint only reads workflows — handed an action.yml it reports a
      # workflow with no `jobs`. So the composites are checked here for what
      # silently breaks them (a run step with no shell never runs the script it
      # carries) and the scripts inlined in them go through the pinned
      # shellcheck, which is the one every gate in this house runs.
      - name: Check the composite actions
        run: |
          set -euo pipefail
          # shellcheck source=/dev/null
          . .github/actions/_lib/shellcheck.sh
          mapfile -t actions < <(git ls-files '.github/actions/*/action.y*ml')
          # The ${} below are JS template literals, not shell expansions.
          # shellcheck disable=SC2016
          bun -e '
            let index = 0;
            for (const file of Bun.argv.slice(1)) {
              const action = Bun.YAML.parse(await Bun.file(file).text());
              const problems = [];
              if (!action.name || !action.description) problems.push("needs a name and a description");
              if (action.runs?.using !== "composite") problems.push("runs.using must be composite");
              for (const step of action.runs?.steps ?? []) {
                if (!step.run) continue;
                if (!step.shell) problems.push(`step "${step.name ?? step.run.split("\n")[0]}" has run without shell`);
                await Bun.write(`${process.env.RUNNER_TEMP}/composite-${index++}.sh`, step.run);
              }
              for (const problem of problems) {
                console.log(`::error file=${file}::${problem}`);
                process.exitCode = 1;
              }
            }
          ' "${actions[@]}"
          # --shell=bash because an extracted run: block has no shebang, and
          # bash is what every `shell: bash` step above declares. The libraries
          # those blocks source carry a directive of their own and are read by
          # the shell-scripts gate, which runs over the tracked tree.
          "$SHELLCHECK" --norc --shell=bash "$RUNNER_TEMP"/composite-*.sh

      - name: Validate the Renovate preset
        # Deliberately unpinned: the preset has to hold against the Renovate that runs it today.
        run: npx --yes --package renovate renovate-config-validator --strict --no-global default.json

      - name: Check the Lighthouse budget
        run: |
          set -euo pipefail
          # The ${} below are JS template literals, not shell expansions.
          # shellcheck disable=SC2016
          node -e '
            const assert = require("node:assert/strict");
            const { readFileSync } = require("node:fs");
            const { ci } = JSON.parse(readFileSync("lighthouserc.json", "utf8"));
            assert.ok(ci.collect.staticDistDir, "collect.staticDistDir must name a built site");
            for (const category of ["performance", "accessibility", "best-practices", "seo"]) {
              assert.deepEqual(ci.assert.assertions[`categories:${category}`], ["error", { minScore: 1 }]);
            }
          '
