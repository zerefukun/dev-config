{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Dependency policy shared by zerefukun's Bun projects.",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "schedule": ["* 0-6 * * 1"],
  "timezone": "Europe/Amsterdam",
  "minimumReleaseAge": "7 days",
  "internalChecksFilter": "strict",
  "rangeStrategy": "pin",
  "dependencyDashboard": true,
  "prConcurrentLimit": 3,
  "platformAutomerge": true,
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["* 0-6 * * 1"]
  },
  "packageRules": [
    {
      "description": "Non-major updates land as one automerged PR per week.",
      "matchUpdateTypes": ["patch", "minor", "pin", "digest"],
      "groupName": "non-major dependencies",
      "automerge": true
    },
    {
      "description": "Majors are read before they are merged.",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "This repo's own pins carry executable gate code. Automerging one would land a gate that the adopting PR never showed anybody.",
      "matchPackageNames": ["zerefukun/dev-config"],
      "automerge": false
    },
    {
      "description": "An Expo SDK is one release across every package it ships. A partial bump builds and then fails on a device, so the packages move together and a human reads the SDK's changelog before they do.",
      "matchPackageNames": ["/^expo(-|$)/", "/^@expo\\//", "/^react-native(-|$)/"],
      "groupName": "Expo SDK",
      "automerge": false
    },
    {
      "description": "Better Auth's plugins are versioned in lockstep with the core; a plugin ahead of the core fails at import.",
      "matchPackageNames": ["better-auth", "/^@better-auth\\//"],
      "groupName": "Better Auth"
    },
    {
      "description": "@oxlint/plugins publishes one version per oxlint release and describes that release's AST. The anti-slop rules are typed against it, so the two move together or this repo's typecheck grades the plugin against an AST oxlint is not handing it.",
      "matchPackageNames": ["oxlint", "@oxlint/plugins"],
      "groupName": "oxlint"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Released binaries pinned by tag and archive SHA-256, which no built-in manager reads: in a run: block, or in the shared shell library that fetches a tool more than one caller needs. The renovate: comment above the pair names the dependency, so one manager covers every tool pinned this way — a YAML key or a shell assignment.",
      "managerFilePatterns": [
        "/^\\.github/workflows/[^/]+\\.ya?ml$/",
        "/^\\.github/actions/[^/]+/action\\.ya?ml$/",
        "/^\\.github/actions/_lib/[^/]+\\.sh$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+\\w+_VERSION[:=] ?(?<currentValue>v\\S+)\\s+\\w+_SHA256[:=] ?(?<currentDigest>[0-9a-f]{64})"
      ]
    },
    {
      "customType": "regex",
      "description": "The same gitleaks pin appears unhashed in this repo's local install instructions.",
      "managerFilePatterns": ["/(^|/)README\\.md$/"],
      "matchStrings": ["GITLEAKS_VERSION=(?<currentValue>v\\S+)"],
      "depNameTemplate": "gitleaks/gitleaks",
      "datasourceTemplate": "github-release-attachments"
    }
  ]
}
